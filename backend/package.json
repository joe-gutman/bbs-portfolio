{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for your project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^4.1.0",
    "mongoose": "^6.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
